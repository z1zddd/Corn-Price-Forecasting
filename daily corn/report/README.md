# Experiment Reports

每次实验报告使用以下目录：

```text
<数据名>/<模型名>/<YYYY-MM-DD-HH-MM-SS>/report.md
```

报告目录应与对应的 `results/` 和 `checkpoints/` 使用相同的数据名、模型名和日期时间，便于一一关联。

实验报告至少记录数据版本、模型、参数、预测步长、回看步长、数据划分策略、价格指标、趋势指标、经济指标和主要结论。
