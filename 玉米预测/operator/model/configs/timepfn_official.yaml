name: timepfn_official
class_path: src.models.official.timepfn.TimePFNOfficialForecaster
task: classification
params:
  official_root: third_party/timepfn_official
  checkpoint_path: checkpoints/TimePFN/checkpoint.pth
  pred_len: 1
  close_feature_idx: -1
  patch_size: 16
  embed_dim: 256
  d_model: 1024
  d_ff: 512
  e_layers: 8
  n_heads: 8
  factor: 5
  dropout: 0.1
  activation: gelu
  class_strategy: projection
  use_norm: true
  batch_size: 8
  device: null
