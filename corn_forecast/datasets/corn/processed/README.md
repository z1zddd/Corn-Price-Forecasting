# Processed Corn Materials

Processed materials are cleaned tables ready to be loaded by configs. The
current corn configs still point to root-level CSV files under `datasets/` for
backward compatibility.

This directory documents where package-level processed material definitions
belong. Do not write backtest outputs, metrics, plots, or model artifacts here.
