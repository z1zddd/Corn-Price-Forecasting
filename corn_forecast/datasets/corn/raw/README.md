# Raw Corn Materials

Raw materials are source-like inputs before framework modeling transforms.
Examples include original corn price exports, futures tables, spot price
sources, weather extracts, or vendor/source snapshots.

Small reproducible examples may remain in the root-level `datasets/` directory
for compatibility. Large raw data, private enterprise feeds, credentials, and
archives should stay outside git, for example under `local_data/`.
